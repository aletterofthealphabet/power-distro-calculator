From the current code, modify it such that there is connector verification (ie edison must connect to edison).
In addition add length calculations. Make connector type a drop down for the name rather than have the user
fill out individual specs. In addition, make sure the delete distro and delete circuit buttons work. they do not work 
currently.